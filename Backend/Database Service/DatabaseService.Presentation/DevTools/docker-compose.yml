version: '3.9'

services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: gitwise-sql
    ports:
      - "1433:1433"
    environment:
      SA_PASSWORD: "Rollo!234"
      ACCEPT_EULA: "Y"
    restart: unless-stopped
